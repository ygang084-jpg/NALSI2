# 날씨요정단 PRD

| 항목 | 내용 |
|---|---|
| 팀명 | 날씨요정단 |
| 팀원 | 이재림, 강유진 |

## 1. 한 줄 소개

오늘 날씨(기온·체감온도·습도·날씨상태·자외선지수 등)에 맞춰, 사용자의 위치 입력만으로 옷차림을 추천하고(예시 이미지 포함), 우산·양산·마스크 등 준비물까지 함께 보여주는 앱. 로그인하면 지난 추천 기록도 다시 볼 수 있다.

## 2. 문제 정의

| 항목 | 내용 |
|---|---|
| 누가 (타깃 사용자) | 날씨에 따라 아침마다 뭘 입을지 고민하는 사람 |
| 어떤 상황에서 | 출근·등교 준비 중 날씨 앱만 보고는 실제로 뭘 입어야 할지 감이 안 오는 상황 |
| 무엇이 불편한가 | 기온, 강수, 미세먼지를 따로따로 확인해야 하고 결국 옷차림 판단은 스스로 해야 해서 번거로움. 또한 잘못 입어서 덥거나 추운 불편한 상황이 발생 |

## 3. 핵심 기능

사용자가 위치(현재 위치 또는 지역 검색)만 입력하면, 해당 위치의 날씨 데이터를 기반으로 **코디 추천**과 **준비물 추천**을 함께 보여준다. 로그인한 사용자는 추천 결과가 자동으로 히스토리에 쌓이고, 언제든 다시 조회할 수 있다.

### 3-1. 위치 입력 & 옷 사진 업로드 (선택)
- 현재 위치 버튼(브라우저 Geolocation API) 또는 지역명 검색창
- 옷 사진은 선택 사항이며, 업로드 여부와 무관하게 "오늘의 코디 추천" 버튼은 항상 활성화된다
- 옷 사진 업로드 시 "보유 옷 코디" / 미업로드 시 "기본 코디" 모드 문구를 표시하지만, 실제 추천 로직은 날씨 데이터만으로 판단하고 사진 자체를 AI에 전달하지 않는다

### 3-2. 코디 추천
- **입력**: 기온, 체감온도, 습도, 날씨상태
- **출력**:
  - 오늘 어울리는 옷차림을 한 문장으로 추천 (예: "얇은 니트 + 바람막이, 우산 챙기세요")
  - 추천 코디에 어울리는 예시 이미지 (사용자의 실제 옷 사진은 사용하지 않고, 날씨 데이터만으로 판단한 가상의 코디 이미지)

### 3-3. 준비물 추천
- **입력**: 강수확률, 자외선지수, 미세먼지 수치(PM2.5/PM10)
- **출력**: 우산·양산·마스크 등 필요 여부를 리스트 형태로 표시 (예: 우산 필요, 마스크 필요 없음)
- **판단 기준** (AI 프롬프트에 명시적으로 고정):
  - 우산: 강수확률(pop) 0.3(30%) 이상 → 필요
  - 양산: 자외선지수(UVI) 6 이상 → 필요
  - 마스크: PM2.5 36㎍/m³ 이상 또는 PM10 81㎍/m³ 이상 → 필요

### 3-4. 결과 화면
"코디 예시 이미지 + 추천 문장 + 준비물 리스트(우산/양산/마스크 등)"를 한 화면에서 함께 제공. 로딩 중에는 "날씨 조회 중 → 코디·준비물 생성 중 → 이미지 생성 중" 단계별 진행 상태를 보여준다. 모바일은 이미지→문장→준비물 순 1열, 데스크톱은 이미지(좌) + 문장·준비물(우) 2단.

### 3-5. 로그인 & 히스토리
- Supabase Auth 이메일/비밀번호 로그인·회원가입
- 로그인한 사용자가 코디 추천을 완료할 때마다 조회 위치, 날씨 데이터, 코디 문장, 준비물 리스트, 생성 이미지(성공한 경우)를 자동 저장
- "히스토리" 탭에서 본인의 지난 기록만 최신순으로 조회 가능 (다른 사용자 기록은 절대 보이지 않음)

## 4. 처리 흐름

1. 사용자가 위치(현재 위치 버튼 또는 지역명 검색)를 입력하고, 옷 사진은 선택적으로 업로드한다.
2. "오늘의 코디 추천" 버튼 클릭 한 번으로 아래 전체 파이프라인이 순차 실행된다.
3. 브라우저는 Supabase Edge Function `weather`를 호출한다. 이 함수가 서버 쪽에서 OpenWeatherMap 무료 엔드포인트(Current Weather, 5 Day/3 Hour Forecast, UV Index, Air Pollution)를 조합 호출해 기온·체감온도·습도·날씨상태·자외선지수·강수확률·미세먼지(PM2.5/PM10)를 반환한다.
4. 브라우저는 Supabase Edge Function `coordi-recommendation`을 호출한다. 이 함수가 날씨 데이터를 Gemini 텍스트 모델에 전달해 코디 추천 문장과 준비물 체크리스트(근거 포함)를 함께 생성해 반환한다.
5. 브라우저는 Supabase Edge Function `outfit-image`를 호출한다. 이 함수가 4번에서 생성된 코디 문장을 Gemini 이미지 생성 모델에 전달해 예시 이미지를 생성해 반환한다. 실패해도 파이프라인은 멈추지 않고 텍스트 결과만으로 다음 단계로 진행한다.
6. 최종적으로 코디 이미지(있는 경우)·추천 문장·준비물 리스트를 한 화면에 표시한다.
7. 로그인 상태라면 위 결과를 `coordi_history` 테이블에 저장하고, 히스토리 탭에서 다시 조회할 수 있다.

**참고**: 지역명 검색은 아직 좌표 변환(Geocoding)이 연결되어 있지 않아, 현재는 "현재 위치" 버튼으로 얻은 좌표만 파이프라인 실행이 가능하다.

## 5. 필요한 외부 데이터 / API

- **OpenWeatherMap Current Weather API** (무료): 기온, 체감온도, 습도, 날씨상태
- **OpenWeatherMap 5 Day/3 Hour Forecast API** (무료): 강수확률(pop)
- **OpenWeatherMap UV Index API** (무료, 일부 키에서 미제공 시 0으로 대체): 자외선지수
- **OpenWeatherMap Air Pollution API** (무료): 미세먼지 PM2.5/PM10
- **Google Gemini 텍스트 생성 API** (`gemini-3.1-flash-lite`): 코디 추천 문장 + 준비물 체크리스트 생성
- **Google Gemini 이미지 생성 API** (`gemini-2.5-flash-image`): 코디 추천 문장 기반 예시 이미지 생성 (결제 계정 없이는 무료 할당량이 0이라 실패할 수 있음 — 실패 시 텍스트만으로 결과 제공)
- **Supabase Edge Functions**: 위 모든 외부 API 키를 서버 쪽에 숨기는 프록시 (`weather`, `coordi-recommendation`, `outfit-image`)
- **Supabase Auth**: 이메일/비밀번호 로그인
- **Supabase Database**: `coordi_history` 테이블에 사용자별 추천 기록 저장 (RLS로 본인 데이터만 조회/수정 가능)

당초 계획했던 OpenWeatherMap Geocoding API와 One Call API 3.0은 아직 연동되지 않았다. Geocoding은 지역명 검색 좌표 변환이 아직 없고, One Call 3.0은 결제(구독)가 필요해 무료 엔드포인트 조합으로 대체했다.

## 6. 제약 사항

- 사용자의 실제 옷 사진은 사용하지 않는다 — 순수하게 날씨 데이터만으로 코디를 판단하는 구조. 업로드한 사진은 화면 표시/모드 전환에만 쓰이고 AI에는 전달되지 않는다.
- API 키(OpenWeatherMap, Gemini)는 하드코딩하지 않고 Supabase Edge Function 시크릿(서버 환경변수)으로 관리한다. 클라이언트에는 Supabase 프로젝트 URL과 anon key만 노출된다.
- 하루 완성이 목표인 미니 프로젝트이므로, 이미지 생성이 어려울 경우 이미지 없이 텍스트(코디 문장 + 준비물 리스트)만으로도 완성으로 인정한다.
- 결제(구독/카드 등록) 없이 동작해야 한다 — OpenWeatherMap은 무료 엔드포인트만 사용하고, Gemini 이미지 생성이 무료 할당량 부족으로 실패해도 전체 흐름이 막히지 않아야 한다.
- RLS(Row Level Security)로 `coordi_history`는 본인 소유 행만 조회·수정·삭제 가능하다.

## 7. 오늘의 완성 기준 (Done의 정의)

"위치만 입력해도, AI가 날씨(기온·체감온도·습도·날씨상태·자외선지수)에 맞는 코디를 제안하고, 코디 이미지와 추천 문장, 필요한 준비물을 함께 보여준다. 로그인한 사용자는 이 결과가 히스토리에 남고, 본인의 기록만 다시 조회할 수 있다."
